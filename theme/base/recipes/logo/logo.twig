{% if not nolink %}<a href="{{ paths.root }}">{% endif %}<img src="{{ paths.public }}images/logos/site{% if negative %}-white{% endif %}.png" alt="{{ theme.config.sitename }}" title="{{ theme.config.sitename }}">{% if not nolink %}</a>{% endif %}