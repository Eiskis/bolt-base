{% if not nolink %}<a href="{{ paths.root }}" class="partial-logo">{% endif %}<img src="{{ paths.images }}logos/site{% if negative %}-white{% endif %}.png" alt="{{ theme.config.sitename }}" title="{{ theme.config.sitename }}" class="logo">{% if not nolink %}</a>{% endif %}