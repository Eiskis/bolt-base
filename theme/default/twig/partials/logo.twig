{% if not nolink %}<a href="{{ paths.root }}" class="partial-logo">{% endif %}<img src="{{ paths.theme }}images/logos/omniata{% if negative %}-white{% endif %}.png" alt="Omniata" title="Omniata" class="logo">{% if not nolink %}</a>{% endif %}